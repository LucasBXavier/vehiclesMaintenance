version: '3.9'

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: lucas.bx
      POSTGRES_PASSWORD: 123321ABC
      POSTGRES_DB: maintenance_db
    ports:
      - "5433:5432"